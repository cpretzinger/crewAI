[tool.poetry]
name = "crewai"
version = "0.1.0"
description = ""
authors = ["CP"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
some-package = "^0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
